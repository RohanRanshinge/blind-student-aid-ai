[
  {
    "objectID": "BlindStudentAid.html",
    "href": "BlindStudentAid.html",
    "title": "",
    "section": "",
    "text": "CodeShow All CodeHide All Code"
  },
  {
    "objectID": "BlindStudentAid.html#introduction",
    "href": "BlindStudentAid.html#introduction",
    "title": "",
    "section": "Introduction",
    "text": "Introduction\nThis Colab notebook provides an AI-powered solution to convert handwritten notes into accessible audio formats. Designed primarily for blind students, it leverages Google Cloud’s Vision AI for Optical Character Recognition (OCR), Google’s Gemini Pro for intelligent text correction and formatting, and Google Cloud’s Text-to-Speech (TTS) for natural-sounding audio synthesis.\nThe goal is to transform scanned handwritten pages into structured, grammatically correct text and then into segmented audio files, making educational content more accessible."
  },
  {
    "objectID": "BlindStudentAid.html#how-to-use",
    "href": "BlindStudentAid.html#how-to-use",
    "title": "",
    "section": "How to Use",
    "text": "How to Use\n\nUpload Files: Upload your PDF of handwritten notes and your Google Cloud service account JSON key to your Colab environment. You can drag and drop them into the ‘Files’ tab on the left sidebar.\nGet Gemini API Key: Obtain a Gemini API key from Google AI Studio (aistudio.google.com/build/api-key).\nConfigure Paths and Keys: Update the PDF_FILE_PATH, GOOGLE_CLOUD_SERVICE_ACCOUNT_KEY_PATH, and GEMINI_API_KEY variables in the Configuration section below.\nRun All Cells: Execute all cells in the notebook by going to Runtime &gt; Run all.\nDownload Outputs: Once the process is complete, you will find the generated audio segments (.mp3 files) and the cleaned text file (.txt) in the audio_segments folder in the Colab ‘Files’ tab. You can download them to your local machine.\n\n\n\nCode\n# Install necessary libraries\n!pip install --quiet PyMuPDF pillow\n!pip install --quiet google-cloud-vision\n%pip install --upgrade --quiet google-cloud-texttospeech\n\n\n\n\nCode\nimport os\nimport fitz # PyMuPDF for PDF handling\nfrom PIL import Image\nimport io\nimport re\nimport math\nfrom pydub import AudioSegment\nfrom google.colab import userdata\n\n# Google Cloud API imports\nfrom google.cloud import vision\nfrom google.cloud import texttospeech\nimport google.generativeai as genai\n\n\n\n\nCode\n# --- Configuration ---\n# IMPORTANT: Update these paths with the actual names of your uploaded files\nTITLE='Geography pages 1 to 11'\nPDF_FILE_PATH = '&lt;input file path here&gt;'\n# Ensure your Google Cloud service account JSON key file is uploaded to your Colab files\n# and set this path to its location (e.g., 'service_account.json').\nGOOGLE_CLOUD_SERVICE_ACCOUNT_KEY_PATH = '&lt;google cloud service account file name here&gt;'\n# Gemini API Key: Store this securely in Colab Secrets (Left Panel -&gt; Key icon) under the name 'GEMINI_API_KEY'\nGEMINI_API_KEY = userdata.get('GEMINI_API_KEY')\n\nOUTPUT_IMAGE_FOLDER = \"extracted_images\"\nOUTPUT_AUDIO_FOLDER = \"audio_segments\"\nOUTPUT_TEXT_FILE = \"cleaned_text.txt\" # Added output text file name\nTTS_VOICE_NAME = \"en-IN-Chirp3-HD-Aoede\" # Set to a Chirp3-HD voice from en-IN\n                                   # Explore others for naturalness: https://cloud.google.com/text-to-speech/docs/voices\nSPEAKING_RATE = 1.0 # Adjust speaking rate (0.25 to 4.0, 1.0 is default)\nAUDIO_SEGMENT_DURATION_MIN = 12 # Desired duration for each audio segment\n\n# Set environment variable for Google Cloud authentication (Vision & TTS)\nos.environ['GOOGLE_APPLICATION_CREDENTIALS'] = GOOGLE_CLOUD_SERVICE_ACCOUNT_KEY_PATH\n\n# Configure Gemini API\ngenai.configure(api_key=GEMINI_API_KEY)"
  },
  {
    "objectID": "BlindStudentAid.html#workflow-steps",
    "href": "BlindStudentAid.html#workflow-steps",
    "title": "",
    "section": "Workflow Steps",
    "text": "Workflow Steps\nThis notebook orchestrates a multi-step AI workflow:\n\nPDF to Images: Each page of the input PDF is converted into a high-resolution image.\nHandwritten Text Recognition (OCR): Google Cloud Vision AI processes these images to extract handwritten text, even from challenging inputs.\nIntelligent Text Correction and Formatting: The extracted text, often containing errors from OCR or original handwriting, is sent to Google’s Gemini Pro model. Gemini corrects grammar and spelling, cleans up inconsistencies, and formats the text into a structured Markdown document with headings and lists.\nText Cleanup for Readability: Further processing converts Markdown headings to bold plain text and bullet points to numbered lists, optimizing the text for both readability and subsequent audio synthesis.\nText-to-Speech Synthesis: The cleaned and corrected text is then converted into natural-sounding speech using Google Cloud Text-to-Speech. The Chirp3-HD voice is used for high-quality output, and bold headings are converted into subtle pauses for better listening comprehension.\nAudio Segmentation: The synthesized audio is split into manageable segments (e.g., 12-minute chunks) for easier navigation and listening, especially for longer documents.\n\n\n\nCode\nclass BlindStudentAidAgent:\n    def __init__(self, pdf_path, image_output_folder, audio_output_folder,\n                 tts_voice, speaking_rate, segment_duration_min):\n\n        self.pdf_path = pdf_path\n        self.image_output_folder = image_output_folder\n        self.audio_output_folder = audio_output_folder\n        self.tts_voice = tts_voice\n        self.speaking_rate = speaking_rate\n        self.segment_duration_min = segment_duration_min\n\n        # Initialize Google Cloud clients\n        self.vision_client = vision.ImageAnnotatorClient()\n        self.tts_client = texttospeech.TextToSpeechClient()\n        self.gemini_model = genai.GenerativeModel('gemini-1.5-flash') # Or 'gemini-1.5-pro' for higher quality/cost\n\n        # Create output directories if they don't exist\n        if not os.path.exists(self.image_output_folder):\n            os.makedirs(self.image_output_folder)\n        if not os.path.exists(self.audio_output_folder):\n            os.makedirs(self.audio_output_folder)\n\n    def extract_images_from_pdf(self):\n        \"\"\"\n        Extracts each page of a PDF as a high-resolution image.\n        \"\"\"\n        print(f\"Extracting images from {self.pdf_path}...\")\n        doc = fitz.open(self.pdf_path)\n        image_paths = []\n        for i in range(len(doc)):\n            page = doc.load_page(i)\n            # Increase resolution significantly for better OCR on handwritten text\n            pix = page.get_pixmap(matrix=fitz.Matrix(4, 4)) # Increased matrix for even higher res\n            image_path = os.path.join(self.image_output_folder, f\"page_{i+1}.png\")\n            pix.save(image_path)\n            image_paths.append(image_path)\n        doc.close()\n        print(f\"Extracted {len(image_paths)} images to {self.image_output_folder}.\")\n        return image_paths\n\n    def recognize_handwritten_text(self, image_path):\n        \"\"\"\n        Recognizes handwritten text from an image using Google Cloud Vision API.\n        \"\"\"\n        with io.open(image_path, 'rb') as image_file:\n            content = image_file.read()\n\n        image = vision.Image(content=content)\n        # Use document_text_detection for dense text like a page of notes\n        response = self.vision_client.document_text_detection(image=image)\n        full_text = response.full_text_annotation.text\n        return full_text\n\n    def correct_text_with_gemini(self, text):\n        \"\"\"\n        Corrects grammar and spelling, cleans up text, and formats with Markdown headings\n        using Gemini API.\n        \"\"\"\n        print(\"Correcting, cleaning, and formatting with Markdown using Gemini API...\")\n        prompt = f\"\"\"\n        The following text was extracted from handwritten notes. It may contain\n        typographical errors, spelling mistakes, and grammatical issues.\n\n        Your task is to:\n        1.  **Correct all spelling and grammar errors.**\n        2.  **Ensure proper sentence structure and punctuation.**\n        3.  **Identify logical sections, topics, and sub-topics within the text.**\n        4.  **Format the corrected text using Markdown headings.**\n            * Use `#` for main sections/chapters.\n            * Use `##` for major subheadings.\n            * Use `###` for minor subheadings.\n            * Do not use headings beyond `###` unless absolutely necessary for complex structures.\n        5.  **Maintain the original meaning and factual content strictly.**\n        6.  **Do NOT add any new information, summarize, or paraphrase beyond correction and structuring.**\n        7.  **Ensure the output is solely the markdown-formatted text, with no conversational filler.**\n\n        Text to correct and format:\n        {text}\n        \"\"\"\n        try:\n            response = self.gemini_model.generate_content(prompt)\n            corrected_and_formatted_text = response.text.strip()\n            print(\"Gemini API correction and Markdown formatting complete.\")\n            return corrected_and_formatted_text\n        except Exception as e:\n            print(f\"Error during Gemini API call for correction and formatting: {e}\")\n            print(\"Returning original text for now. Please check your API key, model, and quota.\")\n            return text # Fallback to original text if API call fails\n\n    def clean_and_format_text(self, text):\n        \"\"\"\n        Performs additional cleanup and formats the text for plain text output with bold headings\n        and numbered lists.\n        \"\"\"\n        print(\"Performing final text cleanup and formatting for plain text with bold headings and numbered lists...\")\n        # Replace common variations of newlines with a consistent newline\n        cleaned_text = text.replace('\\r\\n', '\\n').replace('\\r', '\\n')\n\n        # Remove any leading/trailing whitespace from the entire text\n        cleaned_text = cleaned_text.strip()\n\n        lines = cleaned_text.split('\\n')\n        formatted_lines = []\n        list_counter = 0\n        in_list = False\n\n        for line in lines:\n            stripped_line = line.strip()\n\n            # Convert Markdown headings to bold plain text\n            if stripped_line.startswith(\"### \"):\n                heading_text = stripped_line[4:]\n                formatted_lines.append(f\"**{heading_text}**\\n\") # Add newline after heading\n                in_list = False # Reset list state after a heading\n            elif stripped_line.startswith(\"## \"):\n                heading_text = stripped_line[3:]\n                formatted_lines.append(f\"**{heading_text}**\\n\") # Add newline after heading\n                in_list = False # Reset list state after a heading\n            elif stripped_line.startswith(\"# \"):\n                heading_text = stripped_line[2:]\n                formatted_lines.append(f\"**{heading_text}**\\n\") # Add newline after heading\n                in_list = False # Reset list state after a heading\n            # Convert bullet points (*, -, +) to numbered list items\n            elif stripped_line.startswith(('* ', '- ', '+ ')):\n                if not in_list:\n                    list_counter = 1 # Start new numbered list\n                    in_list = True\n                list_item_text = stripped_line[2:] # Remove bullet and space\n                formatted_lines.append(f\"{list_counter}. {list_item_text}\")\n                list_counter += 1\n            elif stripped_line.startswith(('*. ', '-. ', '+. ')): # Handle bullets followed by a period\n                 if not in_list:\n                    list_counter = 1 # Start new numbered list\n                    in_list = True\n                 list_item_text = stripped_line[3:] # Remove bullet, period and space\n                 formatted_lines.append(f\"{list_counter}. {list_item_text}\")\n                 list_counter += 1\n            # Handle numbered lists already present\n            elif re.match(r'^\\d+\\.\\s', stripped_line):\n                 if not in_list:\n                     in_list = True # Recognize existing numbered list\n                 # Keep the existing numbered list format\n                 formatted_lines.append(stripped_line)\n                 # Reset counter if it's the start of a new numbered list (e.g., starts with 1.)\n                 if stripped_line.startswith('1.'):\n                     list_counter = 2 # Next item should be 2\n                 else:\n                     # Try to infer the next number, or just increment if possible\n                     try:\n                         current_number = int(stripped_line.split('.')[0])\n                         list_counter = current_number + 1\n                     except ValueError:\n                         list_counter += 1 # Fallback to simple increment\n            else:\n                # If the line is not a heading or a list item and we were in a list, end the list\n                if in_list and stripped_line: # Only end list if the line is not empty\n                    in_list = False\n                    list_counter = 0 # Reset counter\n                    formatted_lines.append(\"\") # Add an empty line to separate list from next paragraph\n\n                formatted_lines.append(line)\n\n        cleaned_text = \"\\n\".join(formatted_lines)\n\n        # Ensure no more than two consecutive newlines (for paragraph breaks)\n        cleaned_text = re.sub(r'\\n\\n+', '\\n\\n', cleaned_text)\n\n        print(\"Text cleanup and formatting complete.\")\n        return cleaned_text\n\n    def split_text_into_chunks(self, text, max_bytes):\n        \"\"\"\n        Splits text into chunks that are under the maximum byte limit.\n        Splits by sentences or paragraphs to maintain readability.\n        \"\"\"\n        print(f\"Splitting text into chunks under {max_bytes} bytes...\")\n        chunks = []\n        current_chunk = \"\"\n        # Split by sentences (using common sentence-ending punctuation) or paragraphs (double newline)\n        # This regex split attempts to keep the delimiter with the sentence/paragraph\n        sentences = re.split(r'([.!?]\\s+|\\n\\n)', text)\n\n        for sentence in sentences:\n            # Check if adding the next sentence/paragraph exceeds the limit\n            # Encode the potential next chunk to check its byte size\n            if len((current_chunk + sentence).encode('utf-8')) &gt; max_bytes and current_chunk:\n                chunks.append(current_chunk.strip())\n                current_chunk = sentence.strip()\n            else:\n                current_chunk += sentence\n\n        # Add the last chunk if it's not empty\n        if current_chunk:\n            chunks.append(current_chunk.strip())\n\n        print(f\"Text split into {len(chunks)} chunks.\")\n        return chunks\n\n\n    def synthesize_text_to_audio(self, text, output_filename, max_retries=3):\n        \"\"\"\n        Synthesizes text to an audio file using Google Cloud Text-to-Speech with retry logic.\n        Replaces bold headings with \"[pause]\".\n        \"\"\"\n        print(f\"Synthesizing audio to {output_filename}...\")\n\n        # Replace bold headings with \"[pause]\" as per documentation for Chirp3-HD\n        # We will use \"[pause]\" followed by the heading text\n        text_with_pauses = re.sub(r'\\*\\*(.*?)\\*\\*', r'[pause] \\1', text)\n\n        # Use the 'markup' field for input with pause tags\n        synthesis_input = texttospeech.SynthesisInput(markup=text_with_pauses)\n\n        voice = texttospeech.VoiceSelectionParams(\n            language_code=\"en-IN\",\n            name=self.tts_voice,\n            ssml_gender=texttospeech.SsmlVoiceGender.NEUTRAL\n        )\n\n        audio_config = texttospeech.AudioConfig(\n            audio_encoding=texttospeech.AudioEncoding.MP3,\n            speaking_rate=self.speaking_rate\n        )\n\n        for attempt in range(max_retries):\n            try:\n                response = self.tts_client.synthesize_speech(\n                    input=synthesis_input, voice=voice, audio_config=audio_config\n                )\n\n                with open(output_filename, \"wb\") as out:\n                    out.write(response.audio_content)\n                print(f'Audio content written to file \"{output_filename}\"')\n                return output_filename\n            except Exception as e:\n                print(f\"Attempt {attempt + 1} failed during Text-to-Speech synthesis: {e}\")\n                if attempt &lt; max_retries - 1:\n                    print(f\"Retrying in 2 seconds...\")\n                    import time\n                    time.sleep(2)\n                else:\n                    print(f\"Max retries ({max_retries}) reached. Returning None.\")\n                    return None\n\n\n    def concatenate_audio_files(self, audio_files, output_filename):\n        \"\"\"\n        Concatenates a list of audio files into a single audio file.\n        Only concatenates if there are files to combine.\n        \"\"\"\n        print(f\"Concatenating {len(audio_files)} audio files into {output_filename}...\")\n        if not audio_files:\n            print(\"No audio files to concatenate.\")\n            return None\n\n        combined = AudioSegment.empty()\n        for audio_file in audio_files:\n            try:\n                segment = AudioSegment.from_file(audio_file)\n                combined += segment\n            except Exception as e:\n                print(f\"Error processing audio file {audio_file}: {e}\")\n                # Optionally, decide how to handle corrupted files (skip, log, etc.)\n\n        if not combined.empty():\n            combined.export(output_filename, format=\"mp3\")\n            print(f\"Concatenation complete. Saved to {output_filename}.\")\n            return output_filename\n        else:\n            print(\"Combined audio is empty after processing files.\")\n            return None\n\n\n    def split_audio_by_duration(self, audio_path):\n        \"\"\"\n        Splits an audio file into segments of a specified duration.\n        \"\"\"\n        print(f\"Splitting audio file {audio_path} into {self.segment_duration_min}-minute segments...\")\n        audio = AudioSegment.from_file(audio_path)\n        total_length_ms = len(audio)\n        segment_length_ms = self.segment_duration_min * 60 * 1000\n\n        num_segments = math.ceil(total_length_ms / segment_length_ms)\n\n        segment_paths = []\n        for i in range(num_segments):\n            start_ms = i * segment_length_ms\n            end_ms = min((i + 1) * segment_length_ms, total_length_ms)\n            segment = audio[start_ms:end_ms]\n\n            segment_filename = os.path.join(self.audio_output_folder, f\"notes_part_{i+1}.mp3\")\n            segment.export(segment_filename, format=\"mp3\")\n            segment_paths.append(segment_filename)\n            print(f\"Exported {segment_filename}\")\n        print(\"Audio splitting complete.\")\n        return segment_paths\n\n    def run_workflow(self):\n        \"\"\"\n        Orchestrates the entire agentic workflow.\n        \"\"\"\n        print(\"--- Starting AI Agent Workflow for Blind Students ---\")\n\n        # Step 1: Extract images from PDF\n        image_files = self.extract_images_from_pdf()\n        if not image_files:\n            print(\"No images extracted. Check PDF file path and content.\")\n            return\n\n        # Step 2: Recognize handwritten text from images\n        all_extracted_text = []\n        for i, img_path in enumerate(image_files):\n            print(f\"Recognizing text from {img_path} ({i+1}/{len(image_files)})...\")\n            try:\n                text = self.recognize_handwritten_text(img_path)\n                all_extracted_text.append(text)\n            except Exception as e:\n                print(f\"Error recognizing text from {img_path}: {e}\")\n                all_extracted_text.append(f\"[Error extracting text from {os.path.basename(img_path)}]\") # Placeholder\n        full_document_text = \"\\n\".join(all_extracted_text)\n        print(\"\\n--- Raw Extracted Text (First 500 characters) ---\")\n        print(full_document_text[:500] + \"...\" if len(full_document_text) &gt; 500 else full_document_text)\n\n        # Step 3: Correct grammar and spelling using Gemini\n        corrected_text = self.correct_text_with_gemini(full_document_text)\n        print(\"\\n--- Corrected Text (First 500 characters) ---\")\n        print(corrected_text[:500] + \"...\" if len(corrected_text) &gt; 500 else corrected_text)\n\n        # Step 4: Cleanup and format text\n        final_cleaned_text = self.clean_and_format_text(corrected_text)\n        print(\"\\n--- Final Cleaned & Formatted Text (First 500 characters) ---\")\n        print(final_cleaned_text[:500] + \"...\" if len(final_cleaned_text) &gt; 500 else final_cleaned_text)\n\n        # Add the title as a heading to the cleaned text\n        text_with_title = f\"**{TITLE}**\\n\\n{final_cleaned_text}\"\n\n        # Save the cleaned text with title to a file\n        text_output_path = os.path.join(self.audio_output_folder, OUTPUT_TEXT_FILE)\n        with open(text_output_path, \"w\") as f:\n            f.write(text_with_title)\n        print(f\"\\nCleaned text with title saved to {text_output_path}\")\n\n\n        # Step 5: Split text into chunks and synthesize audio for each chunk\n        text_chunks = self.split_text_into_chunks(text_with_title, 4900) # Use a buffer under 5000 bytes\n        temp_audio_files = []\n        for i, chunk in enumerate(text_chunks):\n            temp_audio_filename = os.path.join(self.audio_output_folder, f\"temp_chunk_{i+1}.mp3\")\n            # Call the updated synthesize_text_to_audio method\n            synthesized_file = self.synthesize_text_to_audio(chunk, temp_audio_filename)\n            if synthesized_file:\n                temp_audio_files.append(synthesized_file)\n            else:\n                print(f\"Skipping concatenation for chunk {i+1} due to synthesis error after multiple retries.\")\n\n\n        # Step 6: Concatenate temporary audio files\n        full_audio_filename = os.path.join(self.audio_output_folder, \"full_notes_audio.mp3\")\n        concatenated_audio_path = self.concatenate_audio_files(temp_audio_files, full_audio_filename)\n\n        # Step 7: Split audio into segments\n        segment_paths = []\n        if concatenated_audio_path:\n            segment_paths = self.split_audio_by_duration(concatenated_audio_path)\n\n            # Clean up temporary audio files\n            print(\"Cleaning up temporary audio files...\")\n            for temp_file in temp_audio_files:\n                try:\n                    os.remove(temp_file)\n                    print(f\"Removed {temp_file}\")\n                except OSError as e:\n                    print(f\"Error removing temporary file {temp_file}: {e}\")\n            print(\"Temporary file cleanup complete.\")\n\n        print(\"\\n--- Workflow complete! ---\")\n        print(f\"Audio segments are saved in the '{self.audio_output_folder}' folder.\")\n        print(f\"Cleaned text is saved to '{text_output_path}'.\")\n        print(\"You can download them from the Colab Files sidebar.\")\n        # Return segment paths if successful, otherwise None or handle error appropriately\n        return segment_paths\n\n\n# --- Main Execution Block ---\nif __name__ == \"__main__\":\n    print(\"Please ensure your PDF file (e.g., 'handwritten_notes.pdf'),\")\n    print(\"your Google Cloud service account JSON key (e.g., 'service_account.json'),\")\n    print(\"and your Gemini API Key are uploaded/set up in Colab.\")\n    print(\"\\nRemember to replace 'your_handwritten_notes.pdf', 'your-service-account-key.json',\")\n    print(\"and 'YOUR_GEMINI_API_KEY' with your actual file names and API key.\")\n    print(\"If you get authentication errors, double-check your API keys and service account roles.\")\n\n    # Create dummy files for demonstration if not uploaded\n    if not os.path.exists(PDF_FILE_PATH):\n        print(f\"Warning: {PDF_FILE_PATH} not found. Creating a dummy PDF for demonstration.\")\n        with fitz.open() as doc:\n            page = doc.new_page()\n            page.insert_text((72, 72), \"This is a sample handwritten note. It has some mispelled words.\")\n            page.insert_text((72, 100), \"This sentence has a grammer error. And also some extra   spaces.\")\n            doc.save(PDF_FILE_PATH)\n\n    # if GEMINI_API_KEY == 'YOUR_GEMINI_API_KEY':\n    #     print(\"\\nERROR: Please replace 'YOUR_GEMINI_API_KEY' with your actual Gemini API key.\")\n    #     print(\"You can get one from Google AI Studio (aistudio.google.com/build/api-key).\")\n    #     # Note: Removed exit() to allow workflow to proceed with dummy files if API key is not set,\n    #     # but Gemini correction will fail gracefully. For full functionality, key is needed.\n\n\n    # Instantiate and run the agent\n    agent = BlindStudentAidAgent(\n        pdf_path=PDF_FILE_PATH,\n        image_output_folder=OUTPUT_IMAGE_FOLDER,\n        audio_output_folder=OUTPUT_AUDIO_FOLDER,\n        tts_voice=TTS_VOICE_NAME,\n        speaking_rate=SPEAKING_RATE,\n        segment_duration_min=AUDIO_SEGMENT_DURATION_MIN\n    )\n    agent.run_workflow()"
  },
  {
    "objectID": "motivation.html",
    "href": "motivation.html",
    "title": "The Mission",
    "section": "",
    "text": "The Union Public Service Commission (UPSC) examination is one of the most rigorous academic pursuits in the world. For many, the journey involves thousands of pages of handwritten notes, quick sketches, and complex diagrams.\nFor visually impaired students, this journey faces a unique bottleneck: accessibility. While printed books are often available in digital or braille formats, the invaluable, personalized “handwritten notes” shared in study groups or created in classrooms remain locked away in a visual format."
  },
  {
    "objectID": "motivation.html#the-inspiration-upsc-and-beyond",
    "href": "motivation.html#the-inspiration-upsc-and-beyond",
    "title": "The Mission",
    "section": "",
    "text": "The Union Public Service Commission (UPSC) examination is one of the most rigorous academic pursuits in the world. For many, the journey involves thousands of pages of handwritten notes, quick sketches, and complex diagrams.\nFor visually impaired students, this journey faces a unique bottleneck: accessibility. While printed books are often available in digital or braille formats, the invaluable, personalized “handwritten notes” shared in study groups or created in classrooms remain locked away in a visual format."
  },
  {
    "objectID": "motivation.html#the-problem",
    "href": "motivation.html#the-problem",
    "title": "The Mission",
    "section": "The Problem",
    "text": "The Problem\n\nOCR Limitations: Standard OCR tools often struggle with the cursive or stylized nature of fast-paced handwriting.\nContextual Errors: Even when text is recognized, the “noise” of incorrect grammar or missing punctuation makes Text-to-Speech (TTS) output confusing and robotic.\nNavigation: Listening to a single 2-hour audio file of a textbook is inefficient for targeted revision."
  },
  {
    "objectID": "motivation.html#the-solution",
    "href": "motivation.html#the-solution",
    "title": "The Mission",
    "section": "The Solution",
    "text": "The Solution\nThis project was built to transform that workflow into a seamless experience:\n\nVision AI: Captures high-resolution detail from handwritten pages.\nGemini Pro: Acts as a “bridge” to correct OCR hallucinations and structure the text into logical Markdown.\nSegmented Audio: Instead of one long file, it generates manageable chapters using high-quality Chirp3-HD voices, making it feel like a professional audiobook.\n\n\n“The goal isn’t just to read text, but to provide a tool that respects the effort a student puts into their notes by making them accessible anywhere, anytime.”"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Project Overview",
    "section": "",
    "text": "Welcome to the Blind Student Aid AI project. This tool leverages Google Cloud’s Vision AI and Gemini Pro to convert handwritten educational materials into structured, natural-sounding audio."
  },
  {
    "objectID": "index.html#motivation",
    "href": "index.html#motivation",
    "title": "Project Overview",
    "section": "🌟 Motivation",
    "text": "🌟 Motivation\nIn India, many bright and capable blind students—including those preparing for highly competitive exams like the UPSC—lack access to digitized study materials. They often rely on volunteers to manually dictate handwritten notes from other students.\nThis process has a significant hurdle: every volunteer has a different accent, cadence, and speaking style. For a student trying to focus on complex subjects for hours, this inconsistency is a major cognitive drain.\nThis project was born to solve that. By using AI to digitize handwriting and synthesize it into a singular, high-definition voice, we provide students with a consistent, “podcast-like” learning experience. Now, a student can listen to 100 pages of notes and know that the voice, speed, and quality will remain identical throughout."
  },
  {
    "objectID": "index.html#key-features",
    "href": "index.html#key-features",
    "title": "Project Overview",
    "section": "🚀 Key Features",
    "text": "🚀 Key Features\n\nHandwriting to Digital: Uses Google Cloud Vision AI to extract text from dense, handwritten PDF scans.\nIntelligent Refinement: Employs Gemini 1.5 to fix OCR errors and structure raw notes into logical Markdown.\nConsistent Audio Experience: Converts text into lifelike speech using Google Cloud TTS (Chirp3-HD).\nPodcast-Style Segmentation: Automatically splits long topics into manageable 12-minute audio “chapters” for easier navigation."
  },
  {
    "objectID": "index.html#technical-stack",
    "href": "index.html#technical-stack",
    "title": "Project Overview",
    "section": "🛠️ Technical Stack",
    "text": "🛠️ Technical Stack\n\nCore: Python, Google Colab\nVision: Google Cloud Vision API (OCR)\nIntelligence: Google Gemini API (Text Correction & Structuring)\nSpeech: Google Cloud Text-to-Speech (Chirp3-HD - en-IN voice)"
  },
  {
    "objectID": "index.html#how-to-use",
    "href": "index.html#how-to-use",
    "title": "Project Overview",
    "section": "📋 How to Use",
    "text": "📋 How to Use\n\nPrerequisites:\n\nA Google Cloud Project with Vision and Text-to-Speech APIs enabled.\nA Gemini API Key from Google AI Studio.\nA service account JSON key for Google Cloud authentication.\n\nSetup:\n\nUpload your handwritten notes (PDF) to the environment.\nStore your GEMINI_API_KEY in your environment secrets.\nUpdate the PDF_FILE_PATH and GOOGLE_CLOUD_SERVICE_ACCOUNT_KEY_PATH in the configuration cell.\n\nExecution:\n\nRun all cells in the BlindStudentAid.ipynb notebook.\nThe workflow will output a cleaned_text.txt file and a folder of .mp3 audio segments."
  },
  {
    "objectID": "index.html#project-structure",
    "href": "index.html#project-structure",
    "title": "Project Overview",
    "section": "📂 Project Structure",
    "text": "📂 Project Structure\n├── BlindStudentAid.ipynb   # Main execution notebook\n├── audio_segments/         # Generated MP3 files (output)\n├── extracted_images/       # Intermediate page scans for OCR\n└── cleaned_text.txt        # The AI-corrected transcript\nCreated to ensure that a student’s ambition is never limited by their access to knowledge."
  }
]